{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm install && npm run build --workspace=apps/web",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "env": {
    "NEXT_PUBLIC_APP_URL": "https://consorteform.com"
  }
}
